version: '2'

##############################################
#                                            #
#   Kraken Docker Stack                      #
#                                            #
##############################################

services:
  mongodb:
    image: mongo
    hostname: mongodb
    ports:
      - "27017:27017"
    networks:
      - kraken

  ticker:
    build: 
      context: api
      dockerfile: Dockerfile
    depends_on:
      - mongodb
    ports:
      - "8070:8070"
    networks:
      - kraken
    environment:
      - HTTP_PORT=8070
      - MONGO_HOST=mongodb
      - QUIET=true

# volumes:
#   kraken-datastore:
#     external:
#       name: kraken-datastore

networks:
  kraken: